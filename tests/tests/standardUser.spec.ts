import { test } from '@playwright/test';

test.describe('Standard User Checkout Flow', () => {
  test('standard user purchase journey placeholder', async ({ page }) => {
    // placeholder for:
    // - login as standard_user
    // - reset app state
    // - add products to cart
    // - complete checkout
    // - verify order success
  });
});
